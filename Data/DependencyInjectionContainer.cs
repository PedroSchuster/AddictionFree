using System;
using System.Collections.Generic;
using System.Text;
using AddictionApp.Services;
using AddictionApp.Views;
using Microsoft.Extensions.DependencyInjection;

namespace App.Services
{
    public static class DependencyInjectionContainer 
    {

      /*  public static IServiceCollection ConfigureServices(this IServiceCollection services)
        {
            try
            {
                services.AddSingleton<AddicitionNotesService>();

                services.AddSingleton<AddictionService>();

                services.AddTransient<HomePage>();

                services.AddTransient<NotesPage>();

                services.AddTransient<ProgressPage>();

                return services;
            }
            catch (Exception)
            {
                Application.Current.MainPage.DisplayAlert("Error", "Não foi possivel concluir essa operação", "Fechar");
                return null;
            }
        }*/

        
    }
}
